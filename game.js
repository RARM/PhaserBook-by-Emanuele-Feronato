window.onload = function() { // The onload event (for the windows) fires after all objects in the DOM hierarchy have finished loading
  var game = new Phaser.Game(); // Creating a new instance of a Phaser game
}
